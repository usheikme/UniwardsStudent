<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.uniwards.uniwards_student">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainScreens.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".Registration.RegisterActivity" />
        <activity android:name=".MainScreens.DashboardActivity" />
        <activity
            android:name=".MainScreens.Popups.Enrol.PopupEnrolActivity"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".MainScreens.Popups.DropEnrolment.PopupDropActivity"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".MainScreens.Popups.Passcode.StudentCode"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".MainScreens.Popups.Passcode.VendorCode"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".MainScreens.Popups.Redemption.SuccessfulRedemption"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".MainScreens.Popups.Redemption.FailedRedemption"
            android:theme="@style/Theme.Transparent" />
        <activity android:name=".MainScreens.Popups.QR.QRScanner"></activity>
    </application>

</manifest>