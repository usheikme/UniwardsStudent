<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="75dp"
    android:layout_marginBottom="75dp"
    android:layout_marginRight="20dp"
    android:layout_marginLeft="20dp"
    android:padding="0dp"
    android:layout_gravity="center"
    android:background="@layout/rounded_bg"
    tools:context="xyz.uniwards.uniwards_student.MainScreens.Popups.Passcode.StudentCode">

    <com.goodiebag.pinview.Pinview
        android:id="@+id/pinview_student"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        app:pinBackground="@color/colorButton"
        app:pinWidth="40dp"
        app:pinHeight="40dp"
        app:pinLength="4"
        android:layout_gravity="center_horizontal"
        android:gravity="center_horizontal"
        android:textColor="@color/colorAccent"
        android:textSize="20sp"
        app:cursorVisible="true"
        app:forceKeyboard="true"
        app:inputType="number"
        app:password="false"/>


    <Button
        android:id="@+id/button_scodefinish"
        android:layout_width="120dp"
        android:layout_height="30dp"
        android:layout_below="@+id/pinview_student"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center_horizontal"
        android:background="@layout/rounded_button"
        android:elevation="10dp"
        android:stateListAnimator="@null"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="20dp"
        android:text="Done"
        app:layout_anchor="@+id/nestedScrollView"
        app:layout_anchorGravity="bottom|center" />

    <TextView
        android:id="@+id/text_studentpass"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="13dp"
        android:text="Student Passcode" />

</RelativeLayout>
